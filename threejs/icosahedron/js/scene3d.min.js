var renderer,scene,camera;var winWidth,winHeight;var loader,controls;function main(){init();render()}window.onload=main;function init(){winWidth=window.innerWidth;winHeight=window.innerHeight;var canvas=document.getElementById("renderCanvas");renderer=new THREE.WebGLRenderer({canvas:canvas});renderer.setSize(winWidth,winHeight);scene=new THREE.Scene;scene.background=new THREE.Color(3355443);scene.add(new THREE.AmbientLight(3355443));var light=new THREE.HemisphereLight(16777147,526368,1);light.position.set(4,10,5);scene.add(light);camera=new THREE.PerspectiveCamera(45,winWidth/winHeight,.1,1e3);camera.position.set(1,1,4);controls=new THREE.OrbitControls(camera);window.addEventListener("resize",onWindowResize,false);loader=new THREE.JSONLoader;var modelPath="assets/models/Icosahedron.json";loader.load(modelPath,handleLoad)}function handleLoad(geometry,materials){var texturePath="assets/textures/Icosahedron.png";var material=new THREE.MeshLambertMaterial({map:(new THREE.TextureLoader).load(texturePath)});var mesh=new THREE.Mesh(geometry,material);scene.add(mesh)}function onWindowResize(){camera.aspect=window.innerWidth/window.innerHeight;camera.updateProjectionMatrix();renderer.setSize(window.innerWidth,window.innerHeight)}function render(){requestAnimationFrame(render);controls.update();renderer.render(scene,camera)}